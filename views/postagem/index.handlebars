<div class="card">
  <div class="card-body">
      <h1>{{postagem.titulo}}</h1>
      <small>Data de puplicação: {{postagem.data}}</small>
      <hr>
      <br>
      {{postagem.conteudo}}
  </div>
</div>